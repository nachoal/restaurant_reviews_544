<%# Based on url order /restaurants/:id/review %>
<div class="container">
  <%= simple_form_for [@restaurant,@review] do |f| %>
    <%= f.input :content, as: :text %>

    <%= f.button :submit, class: 'btn btn-primary' %>
  <% end %>
</div>